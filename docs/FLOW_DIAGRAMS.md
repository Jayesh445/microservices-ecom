# Application Flow Diagrams

## 🎯 Table of Contents

1. [Application Startup Flow](#application-startup-flow)
2. [Request Processing Flow](#request-processing-flow)
3. [JWT Authentication Flow](#jwt-authentication-flow)
4. [OTP Registration Flow](#otp-registration-flow)
5. [Order Creation Flow](#order-creation-flow)
6. [Profile Selection Flow](#profile-selection-flow)

---

## Application Startup Flow

```
┌─────────────────────────────────────────────────────────────┐
│  1. JVM starts DemoApplication.main()                       │
│     @SpringBootApplication                                   │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  2. Spring Boot Auto-Configuration                          │
│     - Scans for @Configuration classes                      │
│     - Detects application.yml & application-dev.yml         │
│     - Sets active profile: dev (default)                    │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  3. Component Scanning                                       │
│     @ComponentScan → Finds all @Component, @Service, etc.   │
│     - UserService, ProductService, AuthService...           │
│     - UserController, ProductController...                  │
│     - UserRepository, ProductRepository...                  │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  4. Configuration Classes                                    │
│     SecurityConfig: JWT filter, authentication manager      │
│     JpaConfig: @EnableJpaAuditing                           │
│     AsyncConfig: @EnableAsync                               │
│     WebConfig: CORS configuration                           │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Database Initialization                                  │
│     - Connect to PostgreSQL (ecommerce_db)                  │
│     - Hibernate reads @Entity classes                       │
│     - Dev: ddl-auto=update → Create/update tables           │
│     - Prod: ddl-auto=validate → Only validate schema        │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  6. Security Filter Chain Initialization                    │
│     JwtAuthenticationFilter registered                      │
│     AuthenticationManager configured                        │
│     Password encoder (BCrypt) ready                         │
└──────────────────────┬──────────────────────────────────────┘
                       ↓
┌─────────────────────────────────────────────────────────────┐
│  7. Embedded Tomcat Server Starts                           │
│     Server listening on port 8080                           │
│     Application ready to accept requests                    │
└─────────────────────────────────────────────────────────────┘
```

---

## Request Processing Flow

```
┌──────────────┐
│  HTTP Client │
│  (Browser/   │
│   Postman)   │
└───────┬──────┘
        │ GET /api/users/1
        │ Authorization: Bearer <token>
        ↓
┌─────────────────────────────────────────────────────────────┐
│  DispatcherServlet (Spring MVC Front Controller)            │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Security Filter Chain                                       │
│  1. JwtAuthenticationFilter                                 │
│     - Extract token from Authorization header               │
│     - Validate token signature & expiration                 │
│     - Load user from database                               │
│     - Set SecurityContext with user details                 │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  @RestController - UserController                           │
│  @PreAuthorize("hasRole('ADMIN')") checks user role         │
│  @GetMapping("/{id}") method matched                        │
│  @PathVariable extracts id=1                                │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  @Service - UserService                                      │
│  Business logic: getUserById(1)                             │
│  @Transactional starts database transaction                 │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  @Repository - UserRepository (JPA)                         │
│  Hibernate generates SQL: SELECT * FROM users WHERE id = 1  │
│  Executes query on PostgreSQL                               │
│  Maps result to User entity                                 │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Response Processing                                         │
│  User entity → UserDTO (if needed)                          │
│  @RestController auto-converts to JSON (Jackson)            │
│  GlobalExceptionHandler catches any exceptions              │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  HTTP Response                                               │
│  {                                                           │
│    "success": true,                                          │
│    "message": "User retrieved successfully",                │
│    "data": { "id": 1, "email": "user@example.com", ... }   │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## JWT Authentication Flow

```
┌──────────────────────────────────────────────────────────────┐
│  1. User sends credentials                                   │
│     POST /api/auth/login/password                            │
│     { "email": "user@example.com", "password": "Pass123" }  │
└───────┬──────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  2. AuthController (@RestController)                        │
│     @Valid validates request body                           │
│     Calls authService.loginWithPassword()                   │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  3. AuthService                                              │
│     authenticationManager.authenticate()                    │
│     ↓                                                        │
│     CustomUserDetailsService.loadUserByUsername()           │
│     ↓                                                        │
│     UserRepository.findByEmail()                            │
│     ↓                                                        │
│     Compare hashed passwords (BCrypt)                       │
└───────┬─────────────────────────────────────────────────────┘
        ↓
        ├─ ❌ Invalid → AuthenticationException
        │
        ↓ ✅ Valid
┌─────────────────────────────────────────────────────────────┐
│  4. Generate Tokens (JwtTokenProvider)                      │
│     Access Token:                                            │
│     - Subject: user email                                    │
│     - Expiration: 15 minutes                                 │
│     - Signature: HMAC-SHA256(secret)                        │
│                                                              │
│     Refresh Token:                                           │
│     - Subject: user email                                    │
│     - Expiration: 7 days                                     │
│     - Signature: HMAC-SHA256(secret)                        │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Save refresh token to database                          │
│     user.setRefreshToken(refreshToken)                      │
│     user.setLastLoginAt(now)                                │
│     userRepository.save(user)                               │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  6. Return tokens to client                                  │
│     {                                                        │
│       "accessToken": "eyJhbGc...",                          │
│       "refreshToken": "eyJhbGc...",                         │
│       "tokenType": "Bearer",                                │
│       "expiresIn": 900,                                      │
│       "user": { ... }                                        │
│     }                                                        │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  7. Client stores tokens                                     │
│     localStorage.setItem('accessToken', token)              │
│     localStorage.setItem('refreshToken', refreshToken)      │
└─────────────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  8. Client makes authenticated requests                     │
│     GET /api/users/profile                                   │
│     Authorization: Bearer eyJhbGc...                        │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  9. JwtAuthenticationFilter                                  │
│     - Extract token from header                             │
│     - jwtTokenProvider.validateToken()                      │
│     - jwtTokenProvider.extractUsername()                    │
│     - Load UserDetails                                       │
│     - Set SecurityContext                                    │
└───────┬─────────────────────────────────────────────────────┘
        ↓
        Request proceeds to controller with authenticated user
```

---

## OTP Registration Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Phase 1: Request OTP                                        │
│  POST /api/auth/register/request-otp                        │
│  { "email": "user@example.com" }                            │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  AuthService.requestRegistrationOtp()                       │
│  1. Check if email already exists                           │
│     - If exists → throw DuplicateResourceException          │
│  2. Create temporary user (status: INACTIVE)                │
│     user = User.builder()                                   │
│            .email(email)                                    │
│            .status(INACTIVE)                                │
│            .build()                                         │
│  3. Save temporary user                                     │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  OtpService.generateAndSendOtp()                            │
│  1. Generate 6-digit random OTP                             │
│     otp = "123456"                                          │
│  2. Set expiry time (5 minutes)                             │
│     otpExpiryTime = now + 5 minutes                         │
│  3. Save to user entity                                     │
│     user.setOtp(otp)                                        │
│     user.setOtpExpiryTime(otpExpiryTime)                   │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  EmailService.sendOtpEmail() @Async                         │
│                                                              │
│  DEV MODE (app.otp.console-output: true):                   │
│  ╔════════════════════════════════════╗                     │
│  ║    DEVELOPMENT MODE - OTP          ║                     │
│  ╠════════════════════════════════════╣                     │
│  ║  Email: user@example.com           ║                     │
│  ║  OTP:   123456                     ║                     │
│  ║  Valid for: 5 minutes              ║                     │
│  ╚════════════════════════════════════╝                     │
│                                                              │
│  PROD MODE:                                                  │
│  Email sent via SMTP (no console output)                    │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Response to client                                          │
│  { "success": true, "message": "OTP sent to your email" }  │
└─────────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│  Phase 2: Verify OTP and Complete Registration              │
│  POST /api/auth/register/verify-otp                         │
│  {                                                           │
│    "email": "user@example.com",                             │
│    "firstName": "John",                                      │
│    "lastName": "Doe",                                        │
│    "otp": "123456"                                           │
│  }                                                           │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  AuthService.registerWithOtp()                              │
│  1. Find temporary user by email                            │
│  2. Call otpService.verifyOtp()                             │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  OtpService.verifyOtp()                                     │
│  1. Check if OTP exists                                     │
│     - If null → return false                                │
│  2. Check if OTP expired                                    │
│     - If now > otpExpiryTime → return false                 │
│  3. Compare OTP strings                                     │
│     - If match → clear OTP & return true                    │
│     - If not match → return false                           │
└───────┬─────────────────────────────────────────────────────┘
        ↓
        ├─ ❌ Invalid OTP → throw IllegalStateException
        │
        ↓ ✅ Valid OTP
┌─────────────────────────────────────────────────────────────┐
│  Complete User Registration                                  │
│  user.setFirstName(firstName)                               │
│  user.setLastName(lastName)                                 │
│  user.setStatus(ACTIVE)                                     │
│  user.setEmailVerified(true)                                │
│  user.setRole(CUSTOMER)                                     │
│  userRepository.save(user)                                  │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Generate JWT Tokens                                         │
│  accessToken = jwtTokenProvider.generateAccessToken()       │
│  refreshToken = jwtTokenProvider.generateRefreshToken()     │
│  user.setRefreshToken(refreshToken)                         │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Send Welcome Email @Async                                   │
│  emailService.sendWelcomeEmail(email, firstName)            │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Return AuthResponse with tokens                            │
│  { "accessToken": "...", "refreshToken": "...", ... }      │
└─────────────────────────────────────────────────────────────┘
```

---

## Order Creation Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Client Request                                              │
│  POST /api/orders                                            │
│  Authorization: Bearer <token>                              │
│  {                                                           │
│    "items": [                                                │
│      { "productId": 1, "quantity": 2 },                     │
│      { "productId": 5, "quantity": 1 }                      │
│    ],                                                        │
│    "shippingAddressId": 3,                                  │
│    "billingAddressId": 3,                                   │
│    "paymentMethod": "CREDIT_CARD"                           │
│  }                                                           │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  OrderController                                             │
│  @PreAuthorize("isAuthenticated()") checks authentication   │
│  @Valid validates request                                    │
│  Get current user from SecurityContext                      │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  OrderService.createOrder() @Transactional                  │
│  (All steps below happen in one database transaction)       │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 1: Validate Stock                                      │
│  For each item:                                              │
│    product = productRepository.findById(productId)          │
│    if (product.stockQuantity < quantity)                    │
│      throw InsufficientStockException                       │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 2: Create Order                                        │
│  order = Order.builder()                                    │
│           .user(currentUser)                                │
│           .status(PENDING)                                  │
│           .orderNumber(generateOrderNumber())               │
│           .shippingAddress(shippingAddress)                 │
│           .billingAddress(billingAddress)                   │
│           .build()                                          │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 3: Create Order Items                                  │
│  For each item:                                              │
│    orderItem = OrderItem.builder()                          │
│                 .order(order)                                │
│                 .product(product)                            │
│                 .quantity(quantity)                          │
│                 .price(product.price)                        │
│                 .build()                                     │
│    order.addOrderItem(orderItem)                            │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 4: Calculate Totals                                    │
│  subtotal = sum of (item.price × item.quantity)             │
│  tax = subtotal × TAX_RATE (0.10)                           │
│  shipping = SHIPPING_COST (10.00)                           │
│  discount = 0.00  (if coupon applied)                       │
│  totalAmount = subtotal + tax + shipping - discount         │
│                                                              │
│  order.setSubtotal(subtotal)                                │
│  order.setTax(tax)                                          │
│  order.setShippingCost(shipping)                            │
│  order.setTotalAmount(totalAmount)                          │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 5: Reduce Product Stock                               │
│  For each item:                                              │
│    product.setStockQuantity(                                │
│      product.getStockQuantity() - quantity                  │
│    )                                                         │
│    productRepository.save(product)                          │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 6: Save Order                                          │
│  order = orderRepository.save(order)                        │
│  (Cascade saves all OrderItems)                             │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 7: Process Payment                                     │
│  payment = paymentService.processPayment(                   │
│    order, paymentMethod, totalAmount                        │
│  )                                                           │
│  - Simulate payment gateway                                 │
│  - Create Payment entity                                    │
│  - Set payment status (COMPLETED/FAILED)                    │
└───────┬─────────────────────────────────────────────────────┘
        ↓
        ├─ ❌ Payment Failed → Rollback entire transaction
        │                      (Stock restored, order not saved)
        │
        ↓ ✅ Payment Success
┌─────────────────────────────────────────────────────────────┐
│  Step 8: Update Order Status                                │
│  order.setStatus(CONFIRMED)                                 │
│  order.setPayment(payment)                                  │
│  orderRepository.save(order)                                │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Step 9: Clear User's Cart                                   │
│  cartService.clearCart(currentUser.getId())                 │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  @Transactional commits all changes to database             │
│  ✅ Order saved                                              │
│  ✅ Stock reduced                                            │
│  ✅ Payment processed                                        │
│  ✅ Cart cleared                                             │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Return OrderResponse                                        │
│  {                                                           │
│    "orderId": 123,                                           │
│    "orderNumber": "ORD-20260101-001",                       │
│    "status": "CONFIRMED",                                    │
│    "totalAmount": 159.99,                                    │
│    ...                                                       │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

---

## Profile Selection Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Application Startup                                         │
│  java -jar app.jar                                          │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Spring Boot looks for active profile                       │
│                                                              │
│  Priority (highest to lowest):                              │
│  1. Command line: -Dspring.profiles.active=prod            │
│  2. Environment: export SPRING_PROFILES_ACTIVE=prod        │
│  3. application.yml: spring.profiles.active: dev           │
│  4. Default: None (uses only application.yml)              │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Load Configuration Files                                    │
│                                                              │
│  Always loaded:                                              │
│  1. application.yml (common config)                         │
│                                                              │
│  Profile-specific (merges with common):                     │
│  2a. If profile=dev → application-dev.yml                   │
│  2b. If profile=prod → application-prod.yml                 │
│                                                              │
│  Profile-specific overrides common config                   │
└───────┬─────────────────────────────────────────────────────┘
        ↓
        ├────────────────────┬────────────────────┐
        ↓                    ↓                    ↓
   DEVELOPMENT          PRODUCTION           NO PROFILE

┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  Dev Profile     │  │  Prod Profile    │  │  No Profile      │
│  Active          │  │  Active          │  │  (Defaults)      │
├──────────────────┤  ├──────────────────┤  ├──────────────────┤
│ ✅ Console OTP   │  │ ✅ Email OTP     │  │ ❌ No config     │
│ ✅ Show SQL      │  │ ❌ Hide SQL      │  │ ❌ Won't start   │
│ ✅ DEBUG logs    │  │ ❌ INFO logs     │  │    (missing DB)  │
│ ✅ Auto-create   │  │ ✅ Validate      │  │                  │
│    DB tables     │  │    schema only   │  │                  │
│ ✅ Hardcoded     │  │ ✅ Env vars      │  │                  │
│    DB creds      │  │    for secrets   │  │                  │
│ ✅ Fast restart  │  │ ✅ Secure config │  │                  │
└──────────────────┘  └──────────────────┘  └──────────────────┘

Example logs showing active profile:
┌─────────────────────────────────────────────────────────────┐
│  The following profiles are active: dev                     │
│                                                              │
│  Dev Mode Configuration:                                     │
│  - Database: postgresql://localhost:5432/ecommerce_db      │
│  - OTP Console Output: ENABLED                              │
│  - SQL Logging: ENABLED                                     │
│  - Log Level: DEBUG                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## Annotation Processing Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Source Code (User.java)                                     │
│                                                              │
│  @Entity                    ← JPA: Map to database table    │
│  @Table(name = "users")     ← JPA: Specify table name       │
│  @Getter                    ← Lombok: Generate getters      │
│  @Setter                    ← Lombok: Generate setters      │
│  @NoArgsConstructor         ← Lombok: Empty constructor     │
│  @AllArgsConstructor        ← Lombok: All fields constructor│
│  @Builder                   ← Lombok: Builder pattern       │
│  public class User {                                         │
│    @Id                      ← JPA: Primary key              │
│    @GeneratedValue          ← JPA: Auto-increment           │
│    private Long id;                                          │
│                                                              │
│    @Column(nullable=false)  ← JPA: Not null constraint      │
│    private String email;                                     │
│  }                                                           │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Compile Time (Maven Build)                                  │
│                                                              │
│  1. Lombok Annotation Processor                             │
│     - Runs before Java compilation                          │
│     - Generates code in memory                              │
│     - Adds: getters, setters, constructors, builders        │
│                                                              │
│  2. Java Compiler                                            │
│     - Compiles enhanced code to .class files                │
└───────┬─────────────────────────────────────────────────────┘
        ↓
┌─────────────────────────────────────────────────────────────┐
│  Runtime (Application Startup)                              │
│                                                              │
│  1. Spring Boot starts                                       │
│  2. Hibernate reads @Entity annotations                     │
│  3. Generates SQL:                                          │
│     CREATE TABLE users (                                    │
│       id BIGSERIAL PRIMARY KEY,                             │
│       email VARCHAR(255) NOT NULL                           │
│     );                                                       │
│  4. Executes SQL on PostgreSQL                              │
│  5. Creates JPA metamodel                                   │
│  6. Ready to persist/query User entities                    │
└─────────────────────────────────────────────────────────────┘
```

---

**End of Flow Diagrams** 🎉

These diagrams show how different parts of the application interact at runtime!
